/**
 * @param {TreeNode} root
 * @param {number} val
 * @return {boolean}
 */
function searchTree(root, val) {
    // 💻 DRIVER: Sua implementação começa aqui!
    // Você pode usar DFS (Recursão) ou BFS (Fila).
    
    return false;
}

module.exports = searchTree;
