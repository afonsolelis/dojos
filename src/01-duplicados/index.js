function temDuplicado(numeros) {
    // 💻 DRIVER: Sua implementação começa aqui!
    // O objetivo inicial é retornar "true" se houver algum número repetido no array.
    // Tente começar com a solução mais simples possível (Força Bruta).
    
    return false;
}

module.exports = temDuplicado;
