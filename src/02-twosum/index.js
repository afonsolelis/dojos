function twoSum(nums, target) {
    // 💻 DRIVER: Sua implementação começa aqui!
    // Retorne um array contendo os "elementos" ou os "índices" que somados dão o target.
    // Exemplo: nums = [2, 7, 11, 15], target = 9 -> Retorna [2, 7] ou índices [0, 1]
    
    return [];
}

module.exports = twoSum;
